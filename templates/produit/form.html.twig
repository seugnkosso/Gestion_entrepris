{% extends 'base.html.twig' %} {% block title %}Produit add{% endblock %} {% block
content %}
<div class="mb-3 text-2xl">ajout de Produit</div>

<p class="text-base text-red-500">{{succes['addSucces']??""}}</p>

{{ form_start(form) }}  
{% if is_granted('ROLE_VENDEUR')%}
  {{form_label(form.qte,"qte")}}
  {{form_widget(form.qte,{
      'attr' : {'min' : 0}
  })}}
{% endif %}  
{{ form_widget(form) }}
<input
  type="submit"
  class="float-right px-4 py-2 bg-gray-500 cursor-pointer rounded-xl"
  value="ajouter"
/>
{{ form_end(form) }}
{% endblock content %}
